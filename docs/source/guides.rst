Guides
======

The algorithm documents live as Markdown under ``docs/``. They were not part of the Sphinx
sources, so they did not appear on the site despite being the most substantial writing in
the repository. These pages include them in place, so the files keep the paths the README
already links to.

.. toctree::
   :maxdepth: 1

   guides/reinforce_pp
   guides/grpo
   guides/vllm_worker
